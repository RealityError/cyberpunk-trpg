# 项目约定

## 文本语言

所有非代码性质的文本默认使用中文，包括：

- 文档内容
- API 文档标题、描述、标签
- 面向用户的错误信息
- 示例数据和示例请求
- Git 提交信息、PR 描述和发布说明

代码性质的名称保持英文，包括：

- Python 模块、类、函数、变量名
- HTTP 路由路径
- JSON 字段名
- Git 分支名和依赖包名称

## 规则书文件

规则书 PDF 可以保存在本地 `docs/` 目录，但不会提交到公开仓库。当前 `.gitignore` 已忽略 `docs/*.pdf`。
